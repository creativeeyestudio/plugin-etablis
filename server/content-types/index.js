'use strict';

const etablissement = require('./etablissement');
const menu = require('./menu');
const channel = require('./channel-manager');

module.exports = {
    etablissement,
    menu,
    channel,
};
